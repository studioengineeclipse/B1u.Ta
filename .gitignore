# Build outputs — the participation probe rebuilds these from source.
dist/
build/
target/
bin/
obj/
*.jar
*.class
*.o
*.a
*.so
node_modules/
__pycache__/
*.pyc
.dart_tool/

# Runtime state that is regenerated, not authored. See state/README.md — the ledger in
# particular is per-deployment history whose integrity guarantee does not survive a merge.
state/participation.json
state/packages/
state/ledger.jsonl
state/executive-handoff.md
